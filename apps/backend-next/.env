# File env là file cấu hình để kết nối từ ứng dụng này sang ứng dụng khác nói chung
# Nó thường chứa các thông tin dạng như là TÀI KHOẢN. Tuyệt đối không public và cái này phải private

# Phân tích các biến trong URL kết nối database SQL Server

# Connection String cho kiểu xác thực SQL Server Authentication
# DATABASE_URL="sqlserver://localhost:1433;database=FashionDb;user=sa;password=YourStrong@Passw0rd;trustServerCertificate=true;encrypt=false"

# Connection String cho kiểu xác thực Windows Authentication
DATABASE_URL="mysql://root:365daband@localhost:3306/FashionDb"
