
export default function Page() {
  return (<main style={{padding:20}}>
    <h1>Fashion API — Next.js + Prisma (SQL Server)</h1>
    <p>Use <code>/api/products</code>, <code>/api/categories</code>, <code>/api/brands</code>.</p>
  </main>);
}
